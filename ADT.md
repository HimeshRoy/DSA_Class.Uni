# ADT - Abstract Data Types
-------------
1. ### _ADT Based on operations, not main implementation_

        array = [1, 2, 3, 4, 5]

- STL = Vector , stack, Queue, list

        vector.push_back()

- `Vector - If we want to add an element invector, so we simply used push_back() operation`


--------------------------------
----------------------------------
# Array 

- Array is a collection of homogenuous element to store similer type of data.

             intArr = [1, 2, 3, 4, 5]

if i want to add character typed data in integer array. so its not possible


### Types of Array - 1D, 2D, nD

1D array - arr[5] = {1,2,3,4,5}

2D - array 

for(int i = 0; i < 2, i++){
        for(int j = 0; )
}