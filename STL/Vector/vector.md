# Vector
### In aray, `arr[5]` = {1,2,3,4,5}; `if want to add one more element then we can't do`.
So we use vecotr for dynamic has and has no size issue.

### `push_back()` to enter element at last of array

#include<vector>
vector <int> vec = {1,2,3}
 |
 |
syntex of vector

